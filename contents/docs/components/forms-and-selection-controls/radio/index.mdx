---
title: Radio
description: "Also known as: Radio button, Option button"
---

## Preview

<div className="w-full bg-primary h-[320px] flex items-center justify-center border border-primary gap-4">
  <input type="radio" id="moon-radio" className="moon-radio" />
</div>

## Resources

<Files
  items={[
    {
      type: "file",
      name: "Figma",
      subtype: "figma",
      href: "https://www.figma.com/community/file/1556987856805388414/mds-radio",
    },
    {
      type: "file",
      name: "CSS",
      subtype: "css",
      href: "https://ui.moondesignsystem.com/?path=/docs/forms-selection-controls-radio--docs",
    },
    {
      type: "file",
      name: "React",
      subtype: "react",
      href: "https://react.moondesignsystem.com/?path=/docs/forms-selection-controls-radio--docs",
    },
    {
      type: "file",
      name: "LiveView",
      subtype: "elixir",
      href: "https://liveview.moondesignsystem.com/forms_&_selection_controls/radio",
    },
  ]}
/>

## Properties

| **Property**       | **Values**                                     | **Default** |
| ------------------ | ---------------------------------------------- | ----------- |
| Selected `Variant` | Default \| Checked                             | Default     |
| State `Variant`    | Default \| Hover \| Focus \| Error \| Disabled | Default     |

## When to use

{<svg width="20" height="20" viewBox="0 0 24 24" fill="none" style={{ display: 'inline', verticalAlign: 'middle' }}><path d="M5 13l4 4L19 7" style={{ stroke: "var(--color-text-positive)" }} strokeWidth="2" /></svg>} Use if users must select only one option from the list. <br/>
{<svg width="20" height="20" viewBox="0 0 24 24" fill="none" style={{ display: 'inline', verticalAlign: 'middle' }}><path d="M5 13l4 4L19 7" style={{ stroke: "var(--color-text-positive)" }} strokeWidth="2" /></svg>} When choices or options are clear and distinct from each other, requiring users to make a deliberate selection. <br/>
{<svg width="20" height="20" viewBox="0 0 24 24" fill="none" style={{ display: 'inline', verticalAlign: 'middle' }}><path d="M6 6L18 18M6 18L18 6" style={{ stroke: "var(--color-text-negative)" }} strokeWidth="2" /></svg>} When there is only one option, use checkbox instead for enabling or disabling a setting. <br/>
{<svg width="20" height="20" viewBox="0 0 24 24" fill="none" style={{ display: 'inline', verticalAlign: 'middle' }}><path d="M6 6L18 18M6 18L18 6" style={{ stroke: "var(--color-text-negative)" }} strokeWidth="2" /></svg>} When multiple options can be selected, use checkbox instead. <br/>
{<svg width="20" height="20" viewBox="0 0 24 24" fill="none" style={{ display: 'inline', verticalAlign: 'middle' }}><path d="M6 6L18 18M6 18L18 6" style={{ stroke: "var(--color-text-negative)" }} strokeWidth="2" /></svg>} When the list of options is too long, use select instead. <br/>

## States

Radio (and all other forms and selection controls) express states. States indicate current condition of the component to someone interacting with it.
Using states ensure clear user feedback, highlighting possible actions or barriers. We have included most common states:

| **State**  | **Context**                                                                                                                                          |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Default`  | Status of the component before the interaction. Also referred to as ‘resting’.                                                                       |
| `Hover`    | When the user moves their cursor over the component, but is not taking the action. Slight visual change can indicate interactivity of the component. |
| `Focus`    | When user selects the component via keyboard navigation, but has not yet taken action on it.                                                         |
| `Error`    | Used when an invalid action occurs. Component may appear red or display helper, signaling an issue that needs resolution.                            |
| `Disabled` | A non-interactive state where the component appears faded or grayed out. It indicates that the action is unavailable at the moment.                  |
